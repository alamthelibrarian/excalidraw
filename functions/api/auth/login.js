const random=()=>btoa(String.fromCharCode(...crypto.getRandomValues(new Uint8Array(24)))).replaceAll("+","-").replaceAll("/","_").replaceAll("=","");
export const onRequestGet=async({request,env})=>{if(!env.GOOGLE_CLIENT_ID)return new Response("GOOGLE_CLIENT_ID belum dikonfigurasi.",{status:503});const state=random(),origin=new URL(request.url).origin,url=new URL("https://accounts.google.com/o/oauth2/v2/auth");url.search=new URLSearchParams({client_id:env.GOOGLE_CLIENT_ID,redirect_uri:origin+"/api/auth/callback",response_type:"code",scope:"openid email profile",state,prompt:"select_account"});return new Response(null,{status:302,headers:{Location:url.toString(),"Set-Cookie":`oauth_state=${state}; Path=/api/auth; HttpOnly; Secure; SameSite=Lax; Max-Age=600`}});};
